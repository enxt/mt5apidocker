__pycache__/
.pytest_cache/
.trash/
.env
logs/
*.db

# The container's SQLite volume (docker-compose.yml).
data/

# Runtime artefacts: the API creates its data and log directories on import,
# so running the tests once leaves both behind.
api/.venv/
api/mt5_api/
api/logs/

# Personal files for the terminal (config/servers.dat): mounted at runtime,
# never committed.
config/*
!config/README.md
